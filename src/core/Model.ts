class Model {
  
}

// function Model (type, id, alias, fields) {
//   Module(id + '@model', [type], function (Model) {
//     var Contructor = function () {
//       Model.call(this, alias, fields)
//     }

//     return Leader.models[id] = oo.inherit(Contructor, Model)
//   })
// }
